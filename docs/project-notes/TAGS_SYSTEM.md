# نظام الوسوم المركزي
## Centralized Tags System

---

## ✅ تم الإنجاز

### 1. **ملف مركزي للوسوم** (`lib/tags-config.ts`)
- جميع الوسوم في مكان واحد
- وسوم إنجليزية + ترجماتها العربية
- دوال مساعدة للترجمة والترتيب

### 2. **ترجمة تلقائية حسب اللغة**
```typescript
sortTagsByLanguage(tags, 'ar') // يترجم جميع الوسوم للعربية
sortTagsByLanguage(tags, 'en') // يترجم جميع الوسوم للإنجليزية
```

### 3. **التطبيق في جميع الصفحات**
- ✅ صفحة تفاصيل القفطان (`ProductDetails.tsx`)
- ✅ كروت القفاطين المشابهة (`ProductDetails.tsx`)
- ✅ صفحة القفاطين (`CollectionPage.tsx`)
- ✅ صفحة المفضلة (`WishlistPage.tsx` via `ProductCard.tsx`)
- ✅ كروت القفاطين (`ProductCard.tsx`)

---

## 📝 كيفية إضافة وسم جديد

### الخطوات:
1. افتح `lib/tags-config.ts`
2. أضف الوسم الإنجليزي في `englishTags`:
```typescript
export const englishTags = [
  'Traditional',
  'Modern',
  'YourNewTag', // ← هنا
];
```

3. أضف الترجمة العربية في `arabicTags` **بنفس الترتيب**:
```typescript
export const arabicTags = [
  'تقليدي',
  'عصري',
  'الوسم الجديد', // ← هنا (نفس الموقع)
];
```

**ملاحظة مهمة**: يجب أن يكون عدد الوسوم الإنجليزية = عدد الوسوم العربية

---

## 🎯 الوسوم المتاحة حالياً

| English | العربية |
|---------|---------|
| Traditional | تقليدي |
| Modern | عصري |
| Elegant | أنيق |
| Casual | كاجوال |
| Formal | رسمي |
| Wedding | زفاف |
| Party | حفلات |
| Summer | صيفي |
| Winter | شتوي |
| Handmade | صناعة يدوية |
| Embroidered | مطرز |
| Silk | حرير |
| Cotton | قطن |
| Velvet | مخمل |
| Luxury | فاخر |
| Classic | كلاسيكي |
| Vintage | عتيق |
| Contemporary | معاصر |
| Modest | محتشم |
| Festive | احتفالي |

---

## 🔧 الدوال المتاحة

### 1. `sortTagsByLanguage(tags, language)`
**الوظيفة**: ترجمة وترتيب الوسوم حسب اللغة
```typescript
const tags = ['تقليدي', 'Modern', 'أنيق'];
sortTagsByLanguage(tags, 'en'); 
// Result: ['Traditional', 'Modern', 'Elegant']

sortTagsByLanguage(tags, 'ar'); 
// Result: ['تقليدي', 'عصري', 'أنيق']
```

### 2. `getTagTranslation(tag, targetLanguage)`
**الوظيفة**: ترجمة وسم واحد
```typescript
getTagTranslation('Traditional', 'ar'); // 'تقليدي'
getTagTranslation('تقليدي', 'en'); // 'Traditional'
```

### 3. `getAllTags(language)`
**الوظيفة**: الحصول على جميع الوسوم المتاحة
```typescript
getAllTags('ar'); // ['تقليدي', 'عصري', ...]
getAllTags('en'); // ['Traditional', 'Modern', ...]
```

### 4. `isArabicTag(tag)` / `isEnglishTag(tag)`
**الوظيفة**: التحقق من لغة الوسم
```typescript
isArabicTag('تقليدي'); // true
isEnglishTag('Traditional'); // true
```

---

## 💡 مثال على الاستخدام

```typescript
import { sortTagsByLanguage } from '@/lib/tags-config';

// في أي مكون
const { i18n } = useTranslation();

// عرض الوسوم مترجمة
{product.tags && (
  <div>
    {sortTagsByLanguage(product.tags, i18n.language as 'ar' | 'en')
      .map(tag => (
        <span key={tag}>{tag}</span>
      ))
    }
  </div>
)}
```

---

## ✅ المميزات

1. **مركزية**: تعديل واحد يؤثر على جميع الصفحات
2. **ترجمة تلقائية**: الوسوم تترجم حسب اللغة المختارة
3. **سهولة الإضافة**: إضافة وسم جديد في مكان واحد فقط
4. **متسقة**: نفس الترجمة في كل مكان
5. **نظيفة**: لا تكرار في الكود

---

## 📊 الملفات المحدثة

1. **lib/tags-config.ts** (جديد) - نظام الوسوم المركزي
2. **components/sections/ProductDetails.tsx** - تفاصيل القفطان + القفاطين المشابهة
3. **components/sections/CollectionPage.tsx** - صفحة القفاطين
4. **components/sections/WishlistPage.tsx** - صفحة المفضلة (عبر ProductCard)
5. **components/ProductCard.tsx** - كروت القفاطين

---

**تاريخ التحديث**: نوفمبر 2024  
**الحالة**: ✅ جاهز للإنتاج
