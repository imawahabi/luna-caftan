# نظام الوسوم ثنائي اللغة
## Bilingual Tags System

**التاريخ**: نوفمبر 2024  
**الهدف**: فصل الوسوم العربية والإنجليزية لتنظيم أفضل

---

## ✅ التحديثات المنفذة

### 1. **قاعدة البيانات** (`prisma/schema.prisma`)
```prisma
model Product {
  ...
  tags    String @default("[]") // JSON string array for Arabic tags
  tagsEn  String @default("[]") // JSON string array for English tags
  ...
}
```

**المميزات**:
- حقلين منفصلين: `tags` للعربية و `tagsEn` للإنجليزية
- كل حقل يخزن JSON array
- تنظيم أفضل ووضوح أكثر

### 2. **Types** (`types/product.ts`)
```typescript
export interface Product {
  ...
  tags?: string[];      // Arabic tags
  tagsEn?: string[];    // English tags
  ...
}
```

### 3. **دالة مساعدة جديدة** (`lib/tags-config.ts`)
```typescript
export function getProductTags(
  product: { tags?: string[]; tagsEn?: string[] }, 
  currentLanguage: 'ar' | 'en'
): string[] {
  if (currentLanguage === 'ar') {
    return product.tags || [];
  } else {
    return product.tagsEn || [];
  }
}
```

**كيف تعمل**:
- تأخذ المنتج واللغة الحالية
- ترجع الوسوم العربية إذا اللغة عربية
- ترجع الوسوم الإنجليزية إذا اللغة إنجليزية

---

## 📍 الملفات المحدثة

### 1. **قاعدة البيانات**
- ✅ `prisma/schema.prisma` - إضافة حقل `tagsEn`

### 2. **Types**
- ✅ `types/product.ts` - إضافة `tagsEn` للـ Product interface

### 3. **Helper Functions**
- ✅ `lib/tags-config.ts` - إضافة `getProductTags()`

### 4. **Components**
- ✅ `components/sections/ProductDetails.tsx`
- ✅ `components/sections/CollectionPage.tsx`
- ✅ `components/ProductCard.tsx`
- ✅ `components/sections/WishlistPage.tsx` (via ProductCard)

### 5. **Admin Pages**
- ✅ `app/admin/dashboard/page.tsx`
- ✅ `app/admin/dashboard/products/page.tsx`

---

## 🎯 كيفية الاستخدام

### في أي مكون:
```typescript
import { getProductTags } from '@/lib/tags-config';
import { useTranslation } from 'react-i18next';

const { i18n } = useTranslation();

// عرض الوسوم حسب اللغة
{getProductTags(product, i18n.language as 'ar' | 'en').map(tag => (
  <span key={tag}>{tag}</span>
))}
```

### في لوحة التحكم (عربي دائماً):
```typescript
{getProductTags(product, 'ar').map(tag => (
  <span key={tag}>{tag}</span>
))}
```

---

## 📊 المقارنة

### ❌ النظام القديم:
```
- حقل واحد فقط (tags)
- وسوم مختلطة (عربي + إنجليزي)
- يحتاج ترجمة معقدة
- صعب الصيانة
```

### ✅ النظام الجديد:
```
- حقلين منفصلين (tags + tagsEn)
- وسوم منظمة حسب اللغة
- لا يحتاج ترجمة
- سهل الصيانة
- أكثر وضوحاً
```

---

## 🔄 خطوات التطبيق

### 1. تحديث قاعدة البيانات
```bash
npx prisma migrate dev --name add_tags_en_field
npx prisma generate
```

### 2. تحديث البيانات الموجودة (إذا لزم الأمر)
سيتم إنشاء script لنقل البيانات القديمة

### 3. تحديث لوحة التحكم
سيتم إضافة حقلين منفصلين في صفحات الإضافة والتعديل

---

## 💡 مثال على البيانات

### قبل:
```json
{
  "tags": ["تقليدي", "Traditional", "أنيق", "Elegant"]
}
```

### بعد:
```json
{
  "tags": ["تقليدي", "أنيق", "فاخر"],
  "tagsEn": ["Traditional", "Elegant", "Luxury"]
}
```

---

## ✅ المميزات

1. **تنظيم أفضل**: كل لغة في حقل منفصل
2. **لا حاجة للترجمة**: الوسوم جاهزة باللغتين
3. **سهولة الصيانة**: تعديل الوسوم أسهل
4. **أداء أفضل**: لا حاجة لمعالجة معقدة
5. **وضوح أكثر**: واضح أي وسوم لأي لغة

---

## 📝 الخطوات القادمة

1. ✅ تحديث قاعدة البيانات
2. ⏳ تحديث صفحة إضافة منتج (حقلين منفصلين)
3. ⏳ تحديث صفحة تعديل منتج (حقلين منفصلين)
4. ⏳ إنشاء migration script للبيانات القديمة

---

**الحالة**: ✅ جاهز للتطبيق  
**تاريخ التحديث**: نوفمبر 2024
