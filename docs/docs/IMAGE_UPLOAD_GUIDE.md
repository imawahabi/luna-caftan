# 📸 دليل نظام رفع الصور - Luna Caftan

## 🎯 نظرة عامة

تم إضافة نظام رفع صور محلي متكامل يسمح برفع الصور مباشرة إلى المشروع بدون الحاجة لخدمات خارجية.

---

## ✨ المميزات الجديدة

### 1. **نظام رفع الصور المحلي** 🖼️
- رفع الصور مباشرة من الكمبيوتر
- حفظ الصور في مجلد `/public/uploads`
- دعم صيغ: JPG, PNG, WEBP, GIF
- حد أقصى لحجم الصورة: 5 ميجابايت
- أسماء ملفات فريدة تلقائياً

### 2. **معرض الصور** 🎨
- عرض جميع الصور المرفوعة
- اختيار الصور من المعرض
- حذف الصور غير المستخدمة
- معاينة الصور قبل الاختيار

### 3. **رابط خارجي (اختياري)** 🔗
- إمكانية استخدام روابط خارجية
- دعم خدمات مثل Imgur, Cloudinary
- مرونة في اختيار مصدر الصورة

---

## 📁 الملفات الجديدة

### 1. **API Endpoint**
```
app/api/upload/route.ts
```
- `POST` - رفع صورة جديدة
- `GET` - جلب قائمة الصور المرفوعة
- `DELETE` - حذف صورة

### 2. **مكون رفع الصور**
```
components/ImageUploader.tsx
```
مكون React قابل لإعادة الاستخدام يوفر:
- واجهة رفع سهلة
- معرض صور
- إدخال رابط خارجي
- معاينة الصورة الحالية

### 3. **مجلد التخزين**
```
public/uploads/
```
مجلد حفظ الصور المرفوعة

---

## 🔧 كيفية الاستخدام

### في صفحة الإعدادات

```tsx
import ImageUploader from '@/components/ImageUploader';

<ImageUploader
  label="شعار الموقع"
  currentValue={formData.logoUrl}
  onImageSelect={(url) => setFormData({ ...formData, logoUrl: url })}
  showGallery={true}
/>
```

### في صفحات المنتجات

يمكن استخدام نفس المكون لرفع صور المنتجات:

```tsx
<ImageUploader
  label="صورة المنتج"
  currentValue={image}
  onImageSelect={(url) => updateImage(index, url)}
  showGallery={true}
/>
```

---

## 🎨 صفحة الإعدادات المحسّنة

### الأقسام الجديدة:

#### 1. **الشعار والبانر** 🎨
- رفع شعار الموقع
- رفع صورة البانر الرئيسية

#### 2. **معلومات الاتصال** 📞
- رقم الهاتف
- رقم الواتساب
- البريد الإلكتروني
- العنوان (عربي/إنجليزي)

#### 3. **وسائل التواصل الاجتماعي** 🌐
- Instagram
- Facebook
- Twitter/X

#### 4. **عن المتجر** ℹ️
- نبذة مفصلة عن المتجر (عربي/إنجليزي)
- رؤية المتجر وأهدافه

---

## 🔐 الأمان والتحقق

### التحقق من نوع الملف
```typescript
const allowedTypes = ['image/jpeg', 'image/jpg', 'image/png', 'image/webp', 'image/gif'];
```

### التحقق من حجم الملف
```typescript
const maxSize = 5 * 1024 * 1024; // 5MB
```

### أسماء ملفات آمنة
```typescript
const filename = `${timestamp}-${originalName.replace(/[^a-zA-Z0-9.-]/g, '_')}`;
```

---

## 📊 API Endpoints

### رفع صورة
```
POST /api/upload
Content-Type: multipart/form-data

Response:
{
  "success": true,
  "url": "/uploads/1699123456789-image.jpg",
  "filename": "1699123456789-image.jpg",
  "size": 245678,
  "type": "image/jpeg"
}
```

### جلب الصور
```
GET /api/upload

Response:
{
  "images": [
    {
      "filename": "1699123456789-image.jpg",
      "url": "/uploads/1699123456789-image.jpg",
      "size": 245678,
      "createdAt": "2024-11-06T10:00:00.000Z"
    }
  ]
}
```

### حذف صورة
```
DELETE /api/upload?filename=1699123456789-image.jpg

Response:
{
  "success": true,
  "message": "تم حذف الصورة بنجاح"
}
```

---

## 🎯 حالات الاستخدام

### 1. **رفع شعار المتجر**
1. اذهب إلى: `/admin/dashboard/settings`
2. في قسم "الشعار والبانر"
3. اضغط "رفع من الجهاز"
4. اختر الصورة
5. احفظ الإعدادات

### 2. **إضافة صور للمنتجات**
1. في صفحة إضافة/تعديل منتج
2. في قسم "الصور"
3. اضغط "رفع من الجهاز" أو "المعرض"
4. اختر الصورة المناسبة

### 3. **استخدام رابط خارجي**
1. اضغط "رابط خارجي"
2. الصق رابط الصورة
3. اضغط "تطبيق"

---

## 🚀 المميزات المستقبلية المقترحة

### قريباً:
- [ ] ضغط الصور تلقائياً
- [ ] تغيير حجم الصور
- [ ] تحسين الصور للويب (WebP)
- [ ] رفع متعدد (Drag & Drop)
- [ ] معاينة مباشرة
- [ ] مجلدات منظمة (products, logos, banners)
- [ ] بحث في المعرض
- [ ] فلترة حسب التاريخ/الحجم

### متقدم:
- [ ] تكامل مع CDN
- [ ] نسخ احتياطي تلقائي
- [ ] تحسين الأداء (lazy loading)
- [ ] إحصائيات استخدام الصور
- [ ] تنظيف تلقائي للصور غير المستخدمة

---

## 📝 ملاحظات مهمة

### ⚠️ تحذيرات
1. **النسخ الاحتياطي**: تأكد من عمل نسخة احتياطية من مجلد `/public/uploads` بانتظام
2. **المساحة**: راقب حجم مجلد الصور لتجنب امتلاء القرص
3. **الأمان**: لا ترفع صور حساسة أو شخصية

### ✅ أفضل الممارسات
1. استخدم أسماء وصفية للصور
2. احذف الصور غير المستخدمة
3. استخدم صيغة WebP للأداء الأفضل
4. ضغط الصور قبل الرفع
5. استخدم أبعاد مناسبة (لا تزيد عن 2000px)

---

## 🔄 التكامل مع الموقع

### استخدام الإعدادات في الموقع

```tsx
import { useSettings } from '@/lib/settings-context';

function MyComponent() {
  const { settings } = useSettings();
  
  return (
    <div>
      <img src={settings.logo_url} alt="Logo" />
      <p>{settings.about_ar}</p>
      <a href={settings.instagram_url}>Instagram</a>
    </div>
  );
}
```

---

## 🎉 الخلاصة

تم إضافة نظام رفع صور محلي متكامل يوفر:
- ✅ رفع مباشر من الكمبيوتر
- ✅ معرض صور منظم
- ✅ دعم الروابط الخارجية
- ✅ واجهة سهلة الاستخدام
- ✅ أمان وتحقق من الملفات
- ✅ تكامل سلس مع الموقع

**الآن يمكنك إدارة جميع صور الموقع بسهولة بدون الحاجة لخدمات خارجية! 🚀**
